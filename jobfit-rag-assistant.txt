Here's a 5-day Phrase 2 Build Plan mapped to your existing Phase 2 learnings (Days 6-10), but as actual project modules — not notebooks.
Phase 2 — Build Plan: 5 Days → JobFit Assistant v1.0
Day 1 — llm.py (LLM Generation) ✅ COMPLETE (2026-06-22)
Concept Applied	Module
Temperature, top-k, top-p, decoding	llm.py
- Pick a local LLM: Microsoft Phi-3-mini (3.8B, best quality for laptop) or SmolLM2 (1.7B, faster)
  → DECISION: SmolLM2-1.7B-Instruct (lighter, runs on CPU, no quantization needed)
- Implement generate_text(prompt, temperature, top_k, top_p) with HuggingFace AutoModelForCausalLM
  → Done in src/jobfit/llm.py — lazy loading, chat template, AutoModelForCausalLM
- Use case: "Given this JD gap, suggest how to improve resume bullet XYZ"
  → Done in analyzer.py lines 61-72 — finds low-matching JD sentences, feeds to generate_text()
- Temperature experiment: Ran 3 temperatures (0.2, 0.7, 1.2) on Colab GPU → outputs in experiments/temperature_comparison.md
- Colab notebook: notebook/test_colab.ipynb — Cells: load model, temperature test, full pipeline
- Key insight: 0.7 is best default — balances coherence with useful variation
Day 2 — prompts.py (Structured Extraction)
Concept Applied	Module
Zero-shot, few-shot, JSON output	prompts.py
- Template library: extract_skills_prompt, summarize_role_prompt, gap_analysis_prompt
- extract_structured_jd(jd_text) → dict (role, skills, experience, tools)
- Few-shot examples for reliable JSON
- Integrate into analyzer.py — add "structured JD" section to report
Day 3 — function_caller.py (Tool Use / Agent Loop)
Concept Applied	Module
Tool calling, JSON schemas	function_caller.py
- Define 3 tool schemas:
- calculate_match_score(resume_path, jd_path)
- summarize_jd(jd_text)
- extract_skills(text)
- Build agent_loop(user_query) → LLM decides which tool → executes → returns result
- This replaces the hardcoded analyzer.py flow with an LLM-driven flow
Day 4 — validator.py (Hallucination Control)
Concept Applied	Module
System prompts, output constraints, grounded responses	validator.py
- validate_against_resume(llm_claim, resume_text) → bool — ensure LLM never claims skills not in resume
- validate_json(output, expected_schema) → bool — ensure structured output matches schema
- System prompt templates with strict constraints
- Add to analyzer.py as a post-processing step
Day 5 — vector_store.py (ChromaDB + RAG)
Concept Applied	Module
Embeddings, vector search, semantic retrieval	vector_store.py
- Store resume bullets + project descriptions as ChromaDB collection
- retrieve(query, k=5) → list[documents] — given a JD requirement, return top-K relevant bullets
- Replace the current similarity.py loop with a retrieval step
- Full RAG pipeline: Retrieve → LLM → Generate insight
Final Integration — analyzer.py v2
The new flow:
JD text
  → extract_structured_jd (prompts.py + llm.py)
  → retrieve relevant resume bullets (vector_store.py)
  → calculate_match_score (function_caller.py)
  → generate gap analysis + suggestions (llm.py + prompts.py)
  → validate against resume (validator.py)
  → print report
Architecture after Phase 2
src/jobfit/
├── __init__.py
├── __main__.py
├── similarity.py       # kept as fallback
├── summarizer.py       # kept as fallback
├── qa.py               # kept as fallback
├── llm.py              # NEW - text generation
├── prompts.py          # NEW - templates + JSON extraction
├── function_caller.py  # NEW - tool-use agent
├── validator.py        # NEW - hallucination checks
├── vector_store.py     # NEW - ChromaDB RAG
└── analyzer.py         # updated orchestrator
Questions before I write this into jobfit-rag-assistant.txt
1. LLM choice: Phi-3-mini (3.8B, best quality) or SmolLM2 (1.7B, faster)? Your laptop can run either.
2. ChromaDB or FAISS: Plan says ChromaDB — good for portfolio (DB features, metadata filtering)?
3. Want the agent loop? Day 3's function-calling agent makes the flow LLM-driven — cool for portfolio, but adds complexity. Worth it?


------------ what i done today day 7
Got it — **Day 7 = Phase 2 Build Day 2 → `prompts.py`**. Colab first, then copy to `src/jobfit/prompts.py`.

Here's your execution roadmap:

---

## Day 7 Execution Plan — `prompts.py`

### Stage 1 — Colab Setup (15 min)
- Open a new notebook: `notebook/day7_prompts.ipynb`
- Mount Drive, install deps (`transformers`, `torch`)
- Import your `llm.py` code (paste it in or load from Drive)
- Load SmolLM2 model once at top

---

### Stage 2 — Build Prompt Templates (30 min)
Write 3 prompt functions in a cell:

```
extract_skills_prompt(jd_text)     → few-shot → JSON
summarize_role_prompt(jd_text)     → zero-shot → 2-line summary  
gap_analysis_prompt(resume, jd)    → zero-shot → bullet gaps
```

Key rule: **few-shot for JSON extraction** (SmolLM2 needs examples to stay on format), zero-shot is fine for summaries.

---

### Stage 3 — Build `extract_structured_jd()` (30 min)
```
extract_structured_jd(jd_text) → dict:
  {
    "role": str,
    "skills": [list],
    "experience": str,
    "tools": [list]
  }
```
Flow inside the function:
```
prompt = extract_skills_prompt(jd_text)
raw_output = generate_text(prompt)        # from llm.py
parsed = parse_json_safe(raw_output)      # strip ```json fences, then json.loads()
return parsed
```

Write `parse_json_safe()` as a helper — strips markdown fences, handles decode errors gracefully.

---

### Stage 4 — Temperature/Few-shot Experiment (20 min)
Run on **2 different JDs** (one technical ML JD, one generic):
- Zero-shot vs few-shot → which gives cleaner JSON?
- Save results to `experiments/prompts_comparison.md`

This is your **key insight cell** for the notebook.

---

### Stage 5 — Copy to Project (15 min)
Once Colab outputs look clean:
- Copy prompt functions → `src/jobfit/prompts.py`
- Copy `parse_json_safe` → same file or `utils.py`
- Update `analyzer.py` — call `extract_structured_jd()` and print a **"Structured JD"** section at the top of the report

---

### Stage 6 — Quick Smoke Test Locally (10 min)
```bash
python -m jobfit --resume data/resume.txt --jd data/jd.txt
```
Check that the Structured JD section appears in the report output.

---

## Notebook Cell Order (Colab)

| Cell | Purpose |
|------|---------|
| 1 | Install + imports |
| 2 | Load SmolLM2 (from llm.py code) |
| 3 | `extract_skills_prompt()` with few-shot |
| 4 | `summarize_role_prompt()` zero-shot |
| 5 | `gap_analysis_prompt()` zero-shot |
| 6 | `parse_json_safe()` helper |
| 7 | `extract_structured_jd()` full function |
| 8 | Test on JD #1 → print dict |
| 9 | Test on JD #2 → print dict |
| 10 | Zero-shot vs few-shot comparison |
| 11 | Save experiment notes |

---

**Estimated total time: ~2 hours**

Want me to write the actual code for Stage 2 and 3 now so you can paste directly into Colab?

this is my plan COMPLETE

Stage 1 — Colab Setup (15 min)

Open a new notebook: notebook/day7_prompts.ipynb
Mount Drive, install deps (transformers, torch)
Import your llm.py code (paste it in or load from Drive)
Load SmolLM2 model once at top


Stage 2 — Build Prompt Templates (30 min)
Write 3 prompt functions in a cell:
extract_skills_prompt(jd_text)     → few-shot → JSON
summarize_role_prompt(jd_text)     → zero-shot → 2-line summary  
gap_analysis_prompt(resume, jd)    → zero-shot → bullet gaps
Key rule: few-shot for JSON extraction (SmolLM2 needs examples to stay on format), zero-shot is fine for summaries.
----
